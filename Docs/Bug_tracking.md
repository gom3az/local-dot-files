# flex — Bug Tracking

Log every error encountered during implementation with root cause + resolution.
Check this file for known issues BEFORE fixing a new error.

| ID | Date | Stage | Error (symptom + command) | Root cause | Resolution | Status |
|----|------|-------|---------------------------|------------|------------|--------|
| B-001 | 2026-09-15 | M0 | `cargo fmt` / `cargo clippy` unavailable: `error: no such command: 'fmt'/'clippy'`; no `rustup`, no sudo for `dnf install rustfmt rust-clippy` | Fedora `rust-1.96.1` split packages; only `cargo`+`rustc` installed in container | RESOLVED 2026-09-15: user-space rustup (`--profile minimal --default-toolchain 1.96.1 --component rustfmt,clippy`, same 1.96.1 as system — no skew); `cargo fmt --check` + `cargo clippy --all-targets -- -D warnings` now run in gates | Closed |
| B-002 | 2026-09-15 | M1 | Same as B-001 at M1 gate time: `cargo fmt --check` and `cargo clippy --all-targets -- -D warnings` still unrunnable (no `rustfmt`/`clippy`, no `rustup`) | Same split-package container as B-001 | RESOLVED 2026-09-15 with B-001 (rustup toolchain). First real clippy run found 34 errors (19 lib + 15 tests), all fixed: `RowFlags` struct + `row_texts` extraction (`draw_row`), `handle_key` split into `handle_esc/pending_delete/armed/chords/special` helpers, mechanical fixes (`map_or`, `clamp`, `writeln!`, separators, `u16::from(!bare)`), intentional-test allows (`unicode_not_nfc` on decomposed fixtures, `struct_excessive_bools` on `RowFlags` with justification). `format_code_in_doc_comments=true` removed from `rustfmt.toml` (nightly-only). Gates: FMT-CLEAN, clippy zero warnings, 61/61 tests green | Closed |
| B-003 | 2026-09-15 | M1 | Spec conflict: task scope orders tiers `prefix-word 100 > prefix 80`; `Docs/Implementation.md` frozen decisions say `prefix 100 > prefix-word 80` | Parallel specs diverged; frozen list not updated with scope | RESOLVED 2026-09-15: string-prefix wins (`TIER_PREFIX=100`, `TIER_PREFIX_WORD=80`) — `"term"` must rank `"Terminal"` above `"Gnome Terminal"`; new `string_prefix_beats_word_prefix` test locks it | Closed |
| B-004 | 2026-09-15 | M2 | `shellcheck` unavailable in container (`which shellcheck` → not found); wrapper gate requires shellcheck-clean `flex-launch.sh` | No `shellcheck` package installed, no sudo (same split-package container as B-001) | OPEN (mitigated 2026-09-15): `bash -n` + `zsh -n` syntax checks pass; wrapper exercised under both shells with stubbed `flex`/`setsid` (GUI/terminal/cancel/malformed paths); `SC2086`-style word-splitting sites carry `shellcheck disable` comments matching `app-cache.sh`. Re-checked 2026-09-15 during M3: still absent; `flex-shot.sh`/`flex-theme.sh` covered by the same mitigation (`bash -n` + `zsh -n` clean, stubbed-pipeline tests in `tests/shot.rs`/`tests/theme.rs`). Re-checked 2026-09-15 during M4: still absent; `flex-clip.sh` covered by the same mitigation (`bash -n` + `zsh -n` clean, stubbed-pipeline tests in `tests/clip.rs` plus a real-binary E2E). Re-checked 2026-09-15 during M5: still absent; `flex-center.sh` covered by the same mitigation (`bash -n` + `zsh -n` clean, stubbed-pipeline tests in `tests/center.rs` plus a live `zsh` dispatch run proving `activate` delivery). Re-checked 2026-09-15 during M6: still absent; `flex-power.sh` covered by the same mitigation (`bash -n` + `zsh -n` clean, `DRY_RUN=1` gate + stubbed-PATH dispatch tests in `tests/power.rs`). Re-checked 2026-09-15 during M7: still absent; `flex-wallpaper.sh` covered by the same mitigation (`bash -n` + `zsh -n` clean, stubbed-pipeline dispatch tests plus the malformed/bad-id/unknown-id refusal cases in `tests/wallpaper.rs`). Re-run `shellcheck flex/wrappers/flex-*.sh` when available | Open |
| B-006 | 2026-09-15 | M3 | `cargo clippy --all-targets -- -D warnings` found 3 errors in new M3 code/tests: `never_loop` in `theme_::json_string_field` (single-pass `loop` + `?`), `useless_format` on stub-script `format!` strings, unused `log` var (stub bodies referenced shell `$log` instead of interpolating the Rust path — real bug: logs would hit an ambiguous redirect); plus `wrapper_dispatches_activate_to_theme_switcher` failed (`activate activate tokyo-night` — stub echoed a prefix the wrapper already passes) | New code written against memory of clippy rules; stub scripts composed without running first | RESOLVED 2026-09-15: JSON scan rewritten as `while let` with `let-else continue` (also skips false-positive key hits inside string values); stubs interpolate `log.display()`; switcher stub logs raw `$@`; gates green (fmt/clippy/test) | Closed |
| B-005 | 2026-09-15 | M2 | New `legacy_preserves_provider_order` test failed: legacy hits `[0,1]` vs expected `[0,1,2]` (`cargo test --lib`) | Test fixture bug, not code: `"Fridge"` is `f-r-i`, which is NOT an ordered subsequence of `"fir"` — legacy correctly rejected it (spec agreed: `score` → `None`) | RESOLVED 2026-09-15: replaced `Fridge` with `My Firm` (prefix-word tier, score 65) so spec ranks `Firefox 100 > My Firm 65 > Confirm 45` while legacy keeps `[0,1,2]`; same fix in `tests/launch.rs` legacy test | Closed |
| B-007 | 2026-09-15 | M4 | New `tests/clip.rs` fixtures failed in parallel: `write hist fixture: No such file or directory` (7 tests) — shared `scratch("corpus")` dir raced `remove_dir_all` against sibling writes | Test-harness bug: every corpus test reused one temp dir name while cargo runs tests in parallel | RESOLVED 2026-09-15: `scratch()` appends an `AtomicU64` sequence so each call owns a unique dir; each test removes its own dir(s) | Closed |
| B-008 | 2026-09-15 | M4 | New `parity_with_bash_pipeline_on_reference_data` failed on live `~/.cache/cliphist` (3185 vs 3185 lines, 2-line content diff): lines with `\r` + invalid bytes differed | Expectation bug, not code: bash preserves invalid bytes raw, Rust surfaces `U+FFFD` per the documented lossy contract (same as launch provider); probe compared raw sets | RESOLVED 2026-09-15: probe lossy-normalizes the bash output (`from_utf8_lossy` per line) before comparing — pins cleaning/dedup/order, which is what the probe owns | Closed |
| B-009 | 2026-09-15 | M5 | First `cargo clippy --all-targets -- -D warnings` on center code found 15 errors in `tests/center.rs` (rustc had only warned): 14× bare `run::replay_keys` setup calls (`#[must_use]`, same class as B-002) + `cast_sign_loss` on `tick as u32` in the soak loop | New test code written without rebinding every replay outcome; loop counter defaulted to `i32` | RESOLVED 2026-09-15: `tap()` helper asserts `Consumed` on setup keystrokes (matches repo bind-and-assert style); soak loop runs `0_u32..60` (no cast). Same run exposed 3 test-data bugs the suite caught correctly: `nmcli` stub matched `$3=="device"` instead of `"DEVICE,TYPE"` (interface discovery silently empty → early `exit 0`); fixture line `Lobby:5:WPA3` lacked the `IN-USE` field (Rust parser skips it; bash would emit a bogus `5` row — fixture fixed to `:Lobby:5:WPA3`); secure-wifi list wrongly marked `*` (wrapper correctly chose disconnect over the password flow — list fixed to disconnected). Gates: FMT-CLEAN, clippy zero warnings, 162/162 tests green | Closed |
| B-010 | 2026-09-15 | M6 | Two M6 harness issues: (1) first clippy run on power/keys additions found 2× `too_many_lines` (pedantic 100-line cap) on `seed_filter_cases`/`seed_tab_cases` after the M6 seed expansion; (2) first `cargo test` run failed `flex_test_seed_table` (`cases.len() >= 30` bound vs 29 actual — estimate drift, not code) | New seed cases pushed both fns past the lint cap; hand-counted bound was off by one (10, not 11, new tab cases) | RESOLVED 2026-09-15: M6 seed cases split into `seed_filter_cases_m6`/`seed_tab_cases_m6` (same concat style as M1 `seed_cases`); bound corrected to `>= 29` (verified via `grep -c`). Gates: FMT-CLEAN, clippy zero warnings, 184/184 tests green (debug + release) | Closed |
| B-011 | 2026-09-15 | M7 | `Terminal::draw(|f| render::render(f, &mut menu))` failed with `expected (), found Option<Rect>` in 8 existing suites after `render` was made to return the reserved preview pane; and `crossterm_backend.writer_mut()` for the out-of-band image write failed with `E0624: method writer_mut is private` (`feature = "backend-writer"` gate) | ratatui 0.29's `Terminal::draw` takes `FnOnce(&mut Frame)` (no return value), so a returning renderer breaks every existing call site; `writer_mut` is behind ratatui's unstable `backend-writer` feature, which `Cargo.toml` deliberately does not enable (pinned `crossterm` feature only) | RESOLVED 2026-09-15: `render` stays `-> ()` and the pane is exposed as a sibling pure function `render::preview_area(area, menu)` fed by a shared `frame_layout` helper (single source of truth, zero churn in existing tests); the image is written to a second `backend::open_tty()` handle — bytes still land in order because `Terminal::draw` flushes before the preview writes | Closed |
| B-012 | 2026-09-15 | M7 | First live kitty run (`kitty -e flex wallpaper`) showed the pane blank for JPEG wallpapers while PNG ones rendered: `mkdir -p ~/.cache/flex/previews` → `Read-only file system`, `FLEX_PREVIEW_CONVERT` probe fine, `magick` conversion fine when pointed at `/tmp` | kitty's graphics protocol cannot carry JPEG/WebP at all: with `t=f` the `f` key is mandatory and only `24`/`32`/`100` (PNG) exist — kitty does **not** sniff the file format for file transmissions (default becomes raw RGBA), unlike `icat`, which converts via ImageMagick. Non-PNG sources therefore need a derived PNG, and that cache lives under `$XDG_CACHE_HOME`, which was not writable in the sandboxed test session | RESOLVED 2026-09-15: `src/preview.rs` transmits PNG sources directly and converts everything else once into `$XDG_CACHE_HOME/flex/previews` (key = path+mtime+size, atomic temp+rename, `FLEX_PREVIEW_CACHE`/`FLEX_PREVIEW_CONVERT` seams); a failing conversion now logs one stderr line instead of failing silently. Verified live in kitty 0.47 on the running Hyprland session: PNG source → 32 651 distinct colors in the pane, converted JPEG → same order once the cache dir is writable, converter disabled → pane stays background while the list is unaffected (mean |list off−on| = 0.2) | Closed |
| B-013 | 2026-09-15 | M7 | New `tests/wallpaper.rs` key-seq tests passed alone but failed in the full parallel run (`keyseq_*` returned `Consumed`, `flex_test_replays_are_deterministic` compared different ids) | Test-harness bug, same class as B-007: `fixture_menu` reused one fixed scratch path per (pid, preview flag) and removed it at the start, so parallel tests deleted each other's fixtures; the determinism test built two menus from two different scratch trees, so the path-hashed ids could never match | RESOLVED 2026-09-15: `scratch()` now appends an `AtomicU64` sequence per call, the fixture tree is deleted right after the menu is built (rows only hold path strings), and the determinism test uses filesystem-free `synthetic_entries()` so both menus carry identical paths | Closed |
| B-014 | 2026-09-15 | M7 | Preview squashed every image: a solid 4:1 test image rendered 568x1096 px (aspect 0.52) inside a 71x69-cell pane, so landscape wallpapers showed as tall smears | kitty **stretches** its payload to fill the `c`x`r` cell box exactly — it does not letterbox the image to preserve its aspect (`docs/graphics-protocol.rst` says otherwise for the placement rectangle, and kitty 0.47 was measured doing the stretch) — and terminal cells are not square (8.0x19.2 px on the reference session), so a pane-shaped box distorts every image. `icat` sidesteps this by doing the aspect arithmetic client-side | RESOLVED 2026-09-15: `preview::fitted_box` walks the whole-cell boxes that fit the pane, keeps the best achievable log-ratio aspect error and returns the largest box within `ASPECT_TOLERANCE` (3 %, since a few percent is invisible and area is not); `preview::png_size` reads the transmitted PNG's `IHDR` for dimensions (std-only, no image crate) and `preview::cell_size` reads cell geometry through crossterm's `TIOCGWINSZ`; `placement` centres the box with whole-cell shifts plus sub-cell `X`/`Y` offsets. A first cut of the search compared scaled errors the wrong way round and picked tiny minimal-error boxes (18x5 cells for a 3:2 photo) — caught by the new unit tests. Live re-measured: 4:1 -> 4.06, 1:4 -> 0.25, 16:9 -> 1.76, and a 3:2 JPEG driven through the ImageMagick converter -> 1.53 (all within 2 %, each centred) | Closed |
| B-015 | 2026-09-15 | M8 | Waybar's network click (`rofi/scripts/wifi.sh`) opened a popup that only printed `nmtui is not installed.` — the "network dialog" could not list or connect anything | The script had been rewritten to `exec nmtui`, but this host has no `NetworkManager-tui` package (`rpm -q NetworkManager-tui` → not installed, no `/usr/bin/nmtui`) and cannot install one (`sudo`: "The no new privileges flag is set"), so the dialog depended on a package the machine cannot have | RESOLVED 2026-09-15: replaced the package dependency with a real provider — `flex wifi` (`src/providers/wifi.rs`, radio + scan rows reusing `center`'s parsers/meta/seam helper) plus `wrappers/flex-wifi.sh` (nmcli radio/disconnect/connect + `/dev/tty` password prompt); Waybar `network.on-click` repointed to the wrapper and `rofi/scripts/wifi.sh` reduced to a delegating stub. Verified live in a pty against the real `nmcli` scan (rows render with signal+security metas, `Esc` exits 130, radio stays `enabled`) plus `tests/wifi.rs` (25) and the `tests/wrappers.rs` entry. Gates: FMT-CLEAN, clippy zero warnings, full suite green | Closed |
| B-016 | 2026-09-15 | M8 | User report: "the first couple seconds the terminal is blank" when opening the network dialog | `wifi::menu` built every row before `run::run` started the TUI, and `nmcli … device wifi list` triggers a real radio scan — measured **2.996 s** on this host (`--rescan no` returns the cached list in **10 ms**) — so the popup showed a blank kitty window until the scan returned; the alt screen is only entered afterwards | RESOLVED 2026-09-15: opening is cached-first. `wifi::menu` builds the first frame from the cached scan and spawns one worker thread for the real scan; `Menu::tick` → `wifi::refresh_scan` swaps its rows in (focus follows the SSID, matched through the filter — an early cut read the focused row by raw index, which is wrong while a filter is active, caught by `refresh_scan_keeps_the_filter_and_the_focused_ssid`). A cold cache shows a dim `Scanning…` row instead of a false `(No Wi-Fi networks)`; `flex-wifi.sh`'s connect probe also uses `--rescan no` so Enter no longer waits for a second scan. Measured live in a pty (stubbed 2 s scan, cold cache): first frame **22 ms**, fresh rows at **3.0 s**; against the real radio, first frame **36 ms** (was ~3 s). Gates: FMT-CLEAN, clippy zero warnings, 319 tests green (debug + release) | Closed |
| B-017 | 2026-09-15 | M8 | User report: selecting a network "does not trigger connect flow and just shows blank tui"; separately, the list "has alignment issues" | Two distinct causes. (1) The secured-network prompt was invisible: `IFS= read -rsp "Password for $ssid: " pw </dev/tty 2>/dev/null` sends `read -p`'s prompt to **stderr**, which was redirected to `/dev/null` (pattern inherited from `flex-center.sh`), so the popup showed an empty screen and looked hung; the next Enter returned an empty password, which the wrapper turned into a silent `exit 0` — no connect, no message, popup gone. (2) The row metas carried `🔒`/`🔓`, which no part of the UI font covers: those resolve to Noto Color Emoji, so inside a row of Noto Sans Nerd Font text the glyph lands at emoji metrics (colour, different size/advance) and visually breaks the meta column. flex's own layout was verified correct first (TestBackend frame dump: every row 79 cells, label col 4, meta right-aligned to the 1-cell pad; `width::str_width("🔒")` = 2, matching the terminfo cells) | RESOLVED 2026-09-15: (1) the prompt is now `printf … >&2` (stderr *is* the popup's terminal — verified live in a pty: `Password for SecureNet:` appears after Enter), the read is silent on `/dev/tty` with a stdin fallback, an empty answer prints `No password entered — not connecting to <ssid>.` instead of exiting silently, and a `Connecting to <ssid>…` status line covers the multi-second `nmcli` connect. (2) wifi rows now use the Nerd Font single-cell icons the deleted rofi picker used (`nf-fa-lock` U+F023 / `nf-fa-unlock` U+F09C, `wifi::net_meta`); `center`'s `wifi_meta_body` keeps its emoji for bash parity, where the metas are not rendered at all (bare rows). (3) Asked which alignment was meant; the answer was "arrange the items — the signal indicator / WPA1 / Connected", so the metas became fixed sub-columns (`wifi::net_meta`): signal padded to `NNN%`, fixed bar, security padded to the widest class in the list, and a reserved `Connected` column — the renderer right-aligns one string per row, so equal widths are what straighten the fields (`network_metas_are_padded_to_one_column_geometry`, `meta_columns_share_one_geometry`). Locked by `wrapper_prints_the_password_prompt_and_never_fails_silently`, `wrapper_reports_progress_for_open_networks`, and the updated meta assertions; `tests/wifi.rs` 35 tests. Gates: FMT-CLEAN, clippy zero warnings, full suite green (debug + release) | Closed |
| B-018 | 2026-09-15 | M8 | User report: "why request password for saved networks?" — selecting a network NetworkManager already has credentials for popped the password prompt instead of connecting | `flex-wifi.sh` decided to prompt from the **scan's** security class alone (`WPA2` ⇒ ask), never consulting the saved profiles, and the picker gave no hint about which networks were saved. `nmcli device wifi connect <ssid>` reuses an existing profile (stored secrets included), so the prompt was both unnecessary and confusing | RESOLVED 2026-09-15: (1) the wrapper now checks the saved profiles first — `is_saved()` reads `nmcli -t -f NAME,TYPE connection show` and matches an `802-11-wireless` profile whose (unescaped) name is the SSID, split against the **last** colon so a name containing `:` survives — and connects from the profile with no prompt; the prompt now only appears when there is no profile, or when the stored credentials are rejected, and that fallback says so (`Saved credentials for <ssid> were rejected — enter the password.`) before asking. (2) the picker shows the state in the meta's own column: `Connected`, `Saved`, or blank (`wifi::state_marker`, `wifi::parse_saved_profiles`, `$WIFI_NMCLI_PROFILES_FILE` seam), so a prompt is never a surprise. `wifi::Snapshot` now carries the four reads (radio/devices/scan/profiles) as one value. Verified live against the real profile list with every mutating `nmcli` call stubbed (`gom3a-5g` is genuinely saved): stderr shows `Connecting to gom3a-5g…` and **no** prompt, the logged call is `nmcli device wifi connect gom3a-5g ifname wlan0`, and the real radio stayed `enabled`/connected. Tests: `wrapper_uses_the_saved_profile_instead_of_asking_for_a_password`, `wrapper_ignores_non_wifi_profiles_when_deciding_to_prompt`, `wrapper_asks_when_the_saved_credentials_are_rejected`, `saved_profiles_parse_from_connection_show`, `saved_profiles_mark_the_rows`, `state_marker_prefers_connected_then_saved` | Closed |
| B-019 | 2026-09-15 | post-M8 | Found while removing the image picker's auto theme: `generate-theme.sh --save-as demo-theme` in a scratch `HOME` saved a theme whose `available/demo-theme/metadata.json` carried `"theme_name": "unsaved"` (a first run with no prior `current/metadata.json` saved no `metadata.json` at all) | `main()` called `save_to_available` **before** `save_metadata`, but `save_to_available` copies `current/` wholesale — so the copy always picked up the *previous* run's metadata (or nothing on a first run). Invisible while the picker minted `auto-<timestamp>` names from the directory name anyway; it surfaced the moment `--save-as` became the only way to persist a theme | RESOLVED 2026-09-15: `save_metadata` now runs before `save_to_available`, so the saved theme carries its own `theme_name` and timestamp. Verified end-to-end in a scratch `HOME` with a stubbed `wal` (pywal16 not installed here): fresh `--save-as demo-theme` → `available/demo-theme/metadata.json` reports `theme_name: demo-theme` (and `current/` agrees); a later unnamed run leaves `available/` at one entry with that theme intact and writes `theme_name: unsaved` to `current/` only | Closed |
| B-020 | 2026-09-15 | review (P1) | `flex launch` and `flex center` print **203 stderr lines before the TUI opens**: unique lines from `flex launch </dev/null 2>&1 >/dev/null` are all `flex: launch: skipping malformed entry <path>`, but classifying them against the live tree gives **202 files with `NoDisplay=true`/`Hidden=true` (skipped by design), 1 unreadable, 0 genuinely malformed** — a 100% false-positive diagnostic stream that also fires on every `flex center` open | `launch::read_entry` (`src/providers/launch.rs:240`) collapses three unrelated outcomes into `None` — unreadable file, missing `Name`/`Exec`, and `NoDisplay`/`Hidden` by design — and `scan_dirs` (`:91`) logs every `None` as "malformed". `tests/launch.rs:44` encodes the same conflation (its name covers all three cases but it asserts only the survivor set), so the suite cannot catch the false message, and `center::launchers_tab` (`center.rs:185`) takes the identical path | RESOLVED 2026-09-15: `launch::read_entry` now returns a three-state `Parsed` enum (`Entry { name, exec, terminal }` / `Hidden` / `Malformed`) instead of `Option`, and `scan_dirs` logs **only** `Malformed`; `find_exec_in` matches `Parsed::Entry { .. }`. Hidden is checked *before* the field check, so a `NoDisplay`/`Hidden` file whose body is also incomplete is still skipped silently rather than reported. Re-measured with the same command on this host: `flex launch </dev/null 2>&1 >/dev/null` went **203 lines → 1**, and that one line is the terminal-init error (`flex: error: flex: cannot open /dev/tty`), not a scan diagnostic — the 202 hidden-by-design entries are silent. Regression test `only_malformed_entries_reach_stderr` (`tests/launch.rs`) runs the real binary against a scratch `HOME` holding one fixture per class and asserts exactly `malformed.desktop` + `noexec-app.desktop` are reported; mutation-checked (re-adding `Hidden` to the log branch makes it report all four). Unit tests `parse_separates_hidden_by_design_from_malformed` and `hidden_wins_over_an_incomplete_body` lock the classification. Gates: FMT-CLEAN, clippy zero warnings, 225 tests green | Closed |
| B-021 | 2026-09-15 | review (P2) | Latent `ACTION:` protocol violation: the id token must be whitespace-free, but `launch` and `theme_` put raw filesystem names in it. A `.desktop` file named `My App.desktop` emits `ACTION: launch My App.desktop …`, so `flex-launch.sh:33`'s `id="${rest%% *}"` reads `My` and the popup dies with `flex-launch: My not found`; a theme directory named `My Theme` reaches `theme-switcher.sh activate "My"`. Not triggered on this host (0 desktop files and 0 theme dirs containing a space), so it is latent, not live | `flex_core::backend::emit_action` writes `ACTION: {provider} {id} {escaped-label}` and all four wrappers extract the id by word-splitting (`flex-launch.sh:33`, `flex-theme.sh:28`, `flex-center.sh:40`, `flex-wifi.sh:24`), so the label is the only free-text field — which is exactly why it is escaped and placed last. Three providers honor this (`wifi.rs:10` keeps the SSID in the label, `wallpaper.rs:19` and `clip.rs:25` hash their ids for this reason) while `launch.rs:114` (`RowId::new(entry.id.clone())`) and `theme_.rs:115` (`RowId::new(entry.name.clone())`) do not; `center::launch_id` (`center.rs:104`) inherits it | RESOLVED 2026-09-15 (hash + `--resolve`, the precedent the review proposed): `launch` and `theme` rows now carry `content_hash_hex` ids of the desktop-id / theme name (`launch::entry_id`, `theme_::entry_id`) and gained hidden `flex launch --resolve <id>` / `flex theme --resolve <id>` lookups that print the real desktop-id / theme name. `flex-launch.sh`, `flex-theme.sh` and `flex-center.sh` (for its `launch:*` rows, now `launch:<hash>`) resolve first, then act; `center::launch_id` prefixes the row hash it is handed. The resolver searches the **same entry set the rows come from**, so an id that cannot be on screen (hidden, malformed, deleted) is refused rather than launched, and unknown ids exit 1 with a single-prefixed message. Verified live: `flex launch --resolve <hash(android-studio.desktop)>` → `android-studio.desktop`; a scratch `HOME` holding `My App.desktop` → `flex launch --resolve <hash>` → `My App.desktop` (the space-bearing case the original bug described); `flex launch --resolve deadbeef` and a hidden entry's hash → `flex: error: launch: unknown id '…'`, exit 1; `flex theme --resolve <hash(catppuccin-mocha)>` → `catppuccin-mocha`. Tests: `space_bearing_desktop_ids_round_trip_through_resolve` and `wrapper_resolves_the_row_hash_before_launching` (`tests/launch.rs`, the latter driving the real `flex-launch.sh` with a stubbed binary and asserting it resolves the hash, launches `setsid -f myapp` from a *space-bearing* desktop-id file, and runs nothing when the id does not resolve); `wrapper_dispatches_activate_to_theme_switcher` (`tests/theme.rs`) now resolves to `Tokyo Night` and asserts `theme-switcher.sh` receives `[activate][Tokyo Night]` — one argument, so a split name cannot pass; `launcher`/`theme` per-row id assertions resolve back to their own labels (`tests/center.rs`, `tests/theme.rs`). Docs updated (`README.md` protocol section, `project_structure.md` convention 4, `Implementation.md`, `CHANGELOG.md`). Gates: FMT-CLEAN, clippy zero warnings, 229 tests green | Closed |
| B-022 | 2026-09-15 | review (P3) | Doubled prefix in the hidden resolve errors: `flex clip --resolve deadbeef` prints `flex: error: flex: clip: unknown id 'deadbeef'`, and `flex wallpaper --resolve deadbeef` prints `flex: error: flex: wallpaper: unknown id 'deadbeef'` | `main` formats every error as `eprintln!("flex: error: {err:#}")` (`src/main.rs:123`), but the `anyhow::bail!` messages at `src/main.rs:184` and `:207` carry their own `flex: clip:` / `flex: wallpaper:` prefix. Every other diagnostic in the crate is a single complete line (`flex: launch: no applications found`), so this one reads as a formatting bug to anyone debugging a stale id | RESOLVED 2026-09-15: the redundant prefix is gone from both messages (`anyhow::bail!("clip: unknown id '{hash}'")`, `anyhow::bail!("wallpaper: unknown id '{id}'")`) and `main` now carries a comment stating that `flex: error:` is added exactly once and that messages bubbling out of `run` must not repeat it. Verified live: `flex clip --resolve deadbeef` → `flex: error: clip: unknown id 'deadbeef'`, `flex wallpaper --resolve deadbeef` → `flex: error: wallpaper: unknown id 'deadbeef'` (both exit 1). Locked by the new test `unknown_resolve_id_is_reported_with_a_single_prefix` in `tests/clip.rs` and `tests/wallpaper.rs`, which run the real binary and compare the whole stderr line. Gates: FMT-CLEAN, clippy zero warnings, 227 tests green. Related: the same doubling still shows for flex-core's own message (`flex: error: flex: cannot open /dev/tty`) — that prefix lives in the engine repo, tracked as B-027 | Closed |
| B-023 | 2026-09-15 | review (P3) | The `.desktop` sort re-runs an allocating key function on every comparison: measured **564 key-function calls for 283 entries** where 283 are needed (`entries.sort_by_key(sort_line)`, `src/providers/launch.rs:97`), and each call allocates three times — a `String` from `sort_line` plus a `Vec<char>` and a `String` from `strip_field_codes` | `sort_by_key` re-invokes the key function per comparison instead of caching it (that is what `sort_by_cached_key` exists for), and the key here is not free: `sort_line` (`launch.rs:247`) formats a `name\texec\tterm` string and `strip_field_codes` (`launch.rs:167`) collects `Vec<char>` before rebuilding a `String`. Startup-only and sub-millisecond, so this is not a live performance problem — it is the wrong API for an allocating key, paid on every `flex launch` and `flex center` open | RESOLVED 2026-09-15: one word — `entries.sort_by_cached_key(sort_line)` — with a comment recording why the cached variant is required for an allocating key. Measured with temporary instrumentation (a counter inside `sort_line`, removed before committing): on the reference tree (84 surviving entries) `sort_by_key` called the key **1054** times and `sort_by_cached_key` calls it **84** (once per entry, 12.5× fewer); on the 4-entry fixture dir the counts are 10 vs 4. The optional byte-indexing rewrite of `strip_field_codes` was deliberately **not** taken: it would save a sub-millisecond startup cost in exchange for a `Vec<u8>` → `String` conversion that needs an unwrap (the pattern is ASCII by construction, so a `expect`/`unwrap_or` panic path would be new); the allocation-per-entry cost that `sort_by_cached_key` removes is the part that scaled with the tree. Gates: FMT-CLEAN, clippy zero warnings, 227 tests green | Closed |
| B-024 | 2026-09-15 | review (P3) | The clipboard history is held twice and then discarded: measured peak RSS **17,772 KB (release) for `flex clip`** versus 3,524 KB for a static provider (`flex shot`), with a **3,611,152-byte / 3227-line** `~/.cache/cliphist` — roughly 14 MB resident above baseline for a picker that renders 120-char previews | `clip::load_entries_in` (`src/providers/clip.rs:106`) clones every cleaned line into a second owned set (`if seen.insert(line.clone())`) while `ClipEntry.full` holds the original, so the whole store is resident twice for the duration of the call; separately `clip_tab()` (`:137`) builds preview labels through `rows()` and then drops every `ClipEntry`, including all of `full`, which it never needed. Memory is proportional to an unbounded user history, and the binary runs inside a kitty popup on a keybind | RESOLVED 2026-09-15: the store is no longer held twice and the tab no longer builds entries it discards. A new private `visit_unique` walks pins-then-reversed-history and dedups on `content_hash_hex` of each line (16 hex chars instead of a second owned copy of every line) while handing the line to a `build` closure by value; `load_entries_in` keeps them, `load_rows_in`/`load`/`clip_tab` preview each line and drop it immediately, and `resolve_in` compares hashes only — it built a full `Vec<ClipEntry>` (full text + preview + `Measured`) to return one line. `entry()` now takes the already-computed hash, so nothing is hashed twice, and `clip_tab`/`tab_from_entries` share one `tab_from_rows`. Measured (release, same call, verified source state on both sides, real 3 611 152-byte / 3227-line store): `flex clip` peak RSS **17 572–17 832 KB → 11 008–11 260 KB** (~6.4 MB less, ~37 %); `flex clip --resolve <id>` **14 720–14 904 KB → 11 064–11 264 KB**. The store is inside a kitty popup on a keybind, so this is paid on every clipboard open. Semantics verified unchanged: the live parity probe against the bash pipeline still agrees (`bash 3204 unique lines, rust 3204 entries`), pins-first/reverse-history/first-occurrence-wins are pinned by the new unit test `row_only_and_entry_paths_agree` (which compares the row-only path with the entry path), and `tests/clip_perf.rs` cold ingest→first frame is 9.6 ms (`cargo test --release --test clip_perf`). The only behavioural difference is a 64-bit hash collision between two distinct lines, which would already have made `--resolve` return the wrong entry. Gates: FMT-CLEAN, clippy zero warnings, 230 tests green | Closed |
| B-025 | 2026-09-15 | review (P3) | `flex center` blocks its own frame loop for **~10.8 ms per idle second** (measured: 5 iterations of the 3 commands = 54 ms) and pays it on every tab, not just `Settings` | `center::refresh_gauges` (`src/providers/center.rs:650`) calls `volume_snapshot()` (1 spawn) and `brightness_snapshot()` (2 spawns) *before* it checks which tab is active, so the cost is paid while the user sits on `Launchers`/`Networks`/`Bluetooth`/`Power`; `Menu::tick` runs the hook inline on the event-loop thread (`flex_core::run::run`, the `poll`-timeout branch), so nothing renders or dispatches for those ~11 ms. Documented as intentional (R7) and modest in absolute terms — flagged only because `wifi` solved the same shape of problem the other way (`menu()` takes a cheap cached first frame and publishes the expensive full scan from a background thread via `PENDING_SCAN`), so the crate now carries both patterns | OPEN — optional. To remove the stall, mirror the `wifi` precedent: read the gauges on a background thread and swap the labels from the tick, reusing the offline/online rewrite `refresh_gauges` already performs. Cheaper: guard on `menu.app.active_tab().name == TAB_SETTINGS`, buying back most of the cost at the price of up to 1 s of staleness when switching to `Settings`. Do not do both | Open |
| B-026 | 2026-09-15 | review (P3) | Empty-provider handling differs four ways. `clip` (`main.rs:190`) and `wallpaper` (`:213`) print a diagnostic and `exit(EXIT_CANCELLED)`; `launch` (`:159`) and `theme` (`:174`) print a diagnostic and then run a TUI with **zero rows**. Verified: `HOME=/nonexistent flex theme` prints `flex: theme: no available themes found` and proceeds to initialise the terminal | An empty row set is a dead end — the menu renders nothing and the user must know to press Esc. The crate already has the better answer in hand: `center::launchers_tab_from_entries` (`center.rs:174`) pushes the bash-exact `(No applications found)` placeholder row, but `flex launch` does not, so the standalone launcher and the center tab disagree about the same underlying scan result | RESOLVED 2026-09-15 (policy chosen: placeholder row, keep the TUI — asked, since B-026 offered two valid policies): the `noop` convention is now shared instead of copied. `providers::NOOP_ID` + `providers::empty_row(label)` are the single definition (`center` re-exports the id it used to own, so `wifi`'s references are unchanged), `launch::tab_rows`/`theme_::tab_rows` append the placeholder when a scan is empty, and `launch_tab`/`theme_tab`/`tab_from_entries` all go through them — so the standalone picker and the `center` Launchers tab show the same row for the same scan (label `(No applications found)`, bash-exact, and `(No themes found)` for themes). `main`'s now-unreachable `no applications found` / `no available themes found` diagnostics were removed: the placeholder is the user-visible signal, and the stderr line could not be seen behind a popup anyway. `flex-launch.sh` and `flex-theme.sh` gained the `noop` arm the other wrappers already had (`[[ "$id" != "noop" ]] || exit 0`, before the id lookup), so `Enter` on the placeholder can never resolve or run anything. The resulting policy is deliberate and now documented: empty *choosers* show a `noop` placeholder (`launch`, `theme`, `center`, `wifi`); empty *data providers whose wrapper notifies and where no action is meaningful* (`clip`, `wallpaper`) keep the diagnostic + `exit(EXIT_CANCELLED)`. Verified live in a real pty (session leader via `pty.fork`, 80x24): `HOME=<empty> flex theme` renders `░(No themes found)` and `Esc` exits 130 — no blank screen. Tests: `empty_scan_shows_the_noop_placeholder` (`tests/launch.rs`, `tests/theme.rs`: the row, the label, and `Enter` selecting it), `wrapper_treats_the_noop_placeholder_as_a_noop` (both files: exit 0, the id is never resolved, nothing is launched/activated — mutation-checked by removing the guard, which fails the tests). Gates: FMT-CLEAN, clippy zero warnings, 234 tests green | Closed |
| B-027 | 2026-09-15 | review (post-B-022) | Found while verifying B-022: the doubled prefix survives for engine-raised errors — the real binary prints `flex: error: flex: cannot open /dev/tty (needs a controlling terminal): No such device or address (os error 6)` (reproduced with `flex launch </dev/null`, exit 1, and it is the **only** stderr line left after B-020) | `main` (`main.rs:123`) adds `flex: error:`, and the message text itself is built in **flex-core** — its TTY error already starts with `flex: `. Unlike B-022 that text is not owned by this repo, so it cannot be fixed by dropping a prefix locally; string surgery in `main` (conditional prefixing) would be worse than the symptom | OPEN — proposed: fix it in the engine (drop the `flex: ` prefix from flex-core's error messages so `main` owns the only one), bump the `flex-core` tag pin and `cargo update -p flex-core`, then lock the line here exactly as B-022's tests do. Not reachable from this repo alone | Open |
